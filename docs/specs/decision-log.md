# Decision Log

Post-baseline decisions and documented errata. Entries amend completed E0 outputs under the rule in [`docs/roadmap/task-execution-rules.md`](../roadmap/task-execution-rules.md) §3: changes to frozen SOT material are made only through an explicit recorded decision. New entries are appended; existing entries are never rewritten.

## D-001 - 2026-08-19 - Logical request payload has a single source of truth

**Decision.** `docs/contracts/hermes-task-contract.md` §2 is the only specification of the logical Hermes task request. The previously duplicated `TaskRequest` field list in `docs/architecture/hermes-integration.md` §5 was removed and now references the contract. A schema was added (`schemas/hermes-task-request.schema.json`), and `schemas/dispatch-intent.schema.json` now constrains `request` with a `$ref` to it. The dispatch intent stores the request verbatim; envelope fields (`dispatch_id`, `route`, `generation`, `idempotency_key`, `content_fingerprint`) are deliberately duplicated inside `request` so the record is self-contained for audit.

**Context.** Review found the same logical request described twice with different field shapes (`route_id` vs `route.id`, `resolved_workspace` vs `resource.workspace`), while the persisted `request` field was unconstrained (`"type": "object"`).

**Consequences.** The nested contract form wins; title, trusted instructions, and receipt instructions remain adapter-rendered templates, not payload fields. Two examples were added (`examples/dispatch-intent.json`, `examples/dispatch-receipt.json`) so every schema has a validating example.

## D-002 - 2026-08-19 - Error category enumerates the exit-code classes

**Decision.** `docs/contracts/error-model.md` now defines `category` as exactly one value per nonzero exit-code class (13 values, 1:1 with exit codes). The stable error code registry is closed for v0.1 and maps every code to a category and exit code, including the five adapter-defined codes (`source_unsupported_version`, `source_missing_required_metadata`, `source_position_unusable`, `source_unsafe_path`, `capability_unsupported`). Exit code 1 is unassigned and must never be emitted; the CLI entry point must recover panics and exit 40 because an unrecovered Go runtime panic exits with status 2 and would be indistinguishable from the usage class.

**Context.** The category value `delivery_unknown` appeared once as an example and was never enumerated; no code-to-exit mapping existed, leaving boundary codes (`path_traversal_rejected`, `quarantine_release_denied`) ambiguous.

**Consequences.** Category and exit code determine each other mechanically. The example error JSON's category changed from `delivery_unknown` to `acceptance_unknown`. `target_response_invalid` is pinned to `acceptance_unknown` per DUR-005.

## D-003 - 2026-08-19 - Traceability matrix is generated, not curated

**Decision.** `docs/specs/traceability-matrix.md` is generated by `scripts/generate-traceability.py` from the Requirements sections of the roadmap, with range and wildcard citations expanded against `required-spec.md`. The script fails on unknown requirement IDs. The manually curated "Primary tasks" column is retired.

**Context.** Mechanical comparison found the curated matrix incorrect: the BND row listed E0-T2, which cites no BND requirement, and the TST row claimed "Every task" while 16 of 32 tasks cite TST requirements.

**Consequences.** Only the Final verification column and the Epic Contribution table remain curated (inside the script and the document respectively). Roadmap edits require rerunning the script.

## D-004 - 2026-08-19 - E3-T3 remains a single task

**Decision.** E3-T3 (retry, unknown reconciliation, dead letter, operator commands) is not split now. Re-evaluate its size when E3 starts, using a recorded decision if a split is needed.

**Context.** Review found E3-T3 unusually large, but the single-active-task rule means a split only changes review cadence, and the roadmap is frozen.

## D-005 - 2026-08-19 - Bundled consistency errata

**Decision.** The following verified inconsistencies were fixed in one pass:

- `reconcile --reason` gains `lost-cursor`; `feedback-loop-and-reconciliation.md` §9 gains an initial-reconciliation row and a mapping sentence between reconciliation types and CLI reason values.
- `repository-layout.md` tree now includes `test/e2e` and `test/helpers`, which the prose already referenced.
- `implementation-guide.md` service list gains `SetRouteActivation` (implements `route enable`/`route disable` with the acknowledged-revision gate) assigned to `application/maintenance`.
- `task-execution-rules.md` DoD applies `go test ./...` only when a Go module exists at that stage.
- `examples/config.yaml` trigger name follows the documented `agent-dispatch.<route-id>.<short-config-digest>` convention.
- E3-T3 and E5-T4 deliverables each gain one line requiring JSON output contracts for the records their commands expose (attempts/dead letters; quarantine/batch/decision).
- `canonical-record-contracts.md` §4 states the request self-containment rule recorded in D-001.

**Context.** Each item was confirmed against the source documents during the design review that produced D-001 through D-004.

## D-006 - 2026-08-19 - E5 and E6 detail freeze is maintained

**Decision.** E5/E6 task detail stays as written. If E0-T4 or E2-T5 verification results require changes (for example webhook payload details or suppression rules), amend through a new decision-log entry rather than pre-weakening the plan now.

**Context.** Review noted tension between the frozen roadmap and pending external verification results.

**Consequences.** No roadmap change today; the amendment path is explicit.

## D-007 - 2026-08-19 - Error model expresses policy-driven unsafe-path quarantine

**Decision.** The stable error code registry gains `unsafe_path_quarantined` (category `quarantined`, exit 5). A boundary note distinguishes the non-durable containment rejection (`source_unsafe_path`, class 30, used when no safe evidence can be stored) from the durable policy quarantine (class 5), and processing-pipeline disposition rule 1 now names the code for each branch.

**Context.** A multi-agent design audit found that `unsafe_path_action` is a required `const: quarantine` in `config.schema.json` while the closed v0.1 registry had no class-5 code applicable to unsafe paths, so the mandated policy outcome was inexpressible without violating either the schema or the error model.

**Consequences.** The registry remains closed; the addition is recorded here and in the changelog per the registry's own change rule.

## D-008 - 2026-08-19 - Follow-up and reconciliation decisions reference generation lineage

**Decision.** `PolicyDecision` gains an optional `generation_lineage { route_id, generation }`; exactly one of `batch_id` or `generation_lineage` is present. Invariant 1 now reads: every decision references either an existing immutable batch or a durable generation lineage. The work completion transaction creates the follow-up policy decision (with generation lineage) in the same commit as the follow-up intent.

**Context.** The invariant required every decision to reference a single immutable batch, but a follow-up covers a dirty generation accumulated across many batches, and scheduled reconciliation has no batch at all. The completion transaction named intent creation but no decision creation, so an implementer had no compliant way to produce a follow-up.

**Consequences.** Audit lineage for follow-ups and reconciliations is explicit, and CON-004 latest-state semantics are preserved: generation lineage identifies why work was requested, not a historical content snapshot.

## D-009 - 2026-08-19 - Route behavior on accepted-then-failed work

**Decision.** The route runtime state machine gains failure transitions: cooperative failure or cancellation with retry budget remaining moves ACTIVE_CLEAN or ACTIVE_DIRTY to FOLLOWUP_READY (one bounded follow-up for latest state, under the same collapse bound as completion); budget exhaustion moves the route to UNCERTAIN for operator resolution. QUARANTINED gains an operator release-or-discard exit edge. `work fail` gains a flag synopsis and a closed v0.1 failure-code set (`agent_error`, `canceled`, `timeout`, `environment_error`), and `work begin` is documented as returning the run ID consumed by `work complete` and `work fail`.

**Context.** The route state machine had no failure transitions, so the changes that activated a route could be silently lost when accepted work failed, contradicting the package's no-silent-loss principle. `work fail` also had no synopsis, and the "stable failure code" requested by the companion skill had no registry.

**Consequences.** FBK-008's "extra bounded follow-up over silent loss" rule now has a defined mechanism on the failure path, and the receipt failure vocabulary is closed like the CLI error registry.

## D-010 - 2026-08-19 - E0 gains Watchman verification task E0-T5

**Decision.** New task E0-T5 verifies the real Watchman public interface and freezes the payload fixture baseline, mirroring E0-T4 for Hermes. E2-T1 depends on E0-T5 and its acceptance cites the frozen baseline; E2-T5 verifies the trigger lifecycle against that baseline and its acceptance references `config validate` instead of the not-yet-existing `doctor`. E1-T2 owns `agent-dispatch init`; E3-T3 owns `route list|show|enable|disable`. The roadmap now has 33 tasks.

**Context.** E2-T1's acceptance required real payload shapes from an E0/E2 verification that only E2-T5 produced, which is circular under the strict linear execution rule. Hermes was verified before code (E0-T4) while the riskiest external input, Watchman, was verified only after the pipeline was implemented; a wrong payload assumption would have reopened all of E2. The audit also found no task owned the `init` and route-management commands required by CLI-004.

**Consequences.** External-interface risk is retired symmetrically in E0. The roadmap summary table was resequenced and the traceability matrix regenerated.

## D-011 - 2026-08-19 - Scheduled reconciliation submits after the production gate

**Decision.** The operations doc now states that pre-gate scheduled reconcile omits `--submit` (audit only) and post-gate recipes add it, matching the CLI contract. Processing-pipeline §7 declares trigger-driven retry liveness intentional for v0.1: due RETRY_WAIT intents are submitted by the next source event or an explicit drain, never by a background timer, and remain visible in `status` and `dispatches list`.

**Context.** The documented daily reconcile command never submitted anything, so scheduled reconciliation could never reach Hermes when no new source events arrived; the `--submit` escape existed in the CLI contract but the operations doc did not say when to use it. The retry liveness dependency was real but undeclared.

**Consequences.** Operators get a correct post-gate recipe, and the trigger-driven tradeoff is now an explicit design position rather than an accidental gap.

## D-012 - 2026-08-19 - Bundled consistency errata

**Decision.** The following verified inconsistencies were fixed in one pass:

- the trigger-name convention `agent-dispatch.<route-id>.<short-config-digest>` is now applied in the configuration-spec example and the source-observation and watchman-environment examples (D-005 had fixed only the config example);
- the watchman-environment example drops the null-valued environment key, which a real environment cannot express;
- the README SOT version aligns with the changelog;
- `Acceptance receipt` is renamed `Dispatch receipt` in the terminology, with DAT-001 aligned to the contract and schema naming;
- the terminology gains definitions for route revision, policy revision, reduced guarantee, capability report, dead letter, one-shot, sink, lease, manifest, settled, and workspace;
- the sink-adapter capability example moves the numeric limit into the schema's `limits` object;
- the configuration template no longer shows empty maps that violate `minProperties: 1`, and the revision-exclusion note references the command-line log level;
- the runbook uninstall step no longer references the post-v0.1 purge operation;
- the examples/scripts README no longer attributes script generation to tasks that do not produce scripts;
- the architecture overview no longer labels Watchman environment input as trusted;
- the nonexistent state token `superseded_pending_reprocess` is replaced with the real `superseded` state;
- the route runtime state machine is persisted as `route_state` on RouteRuntimeState, with `active_dispatch_id` reservation timing defined (reserved at intent creation, cleared at resolution);
- the dispatch state machine's RETRY_WAIT transition now matches the lease-acquisition SQL;
- the production-capable gate reference in acceptance-criteria §3 is gate-based (G0 through G4) instead of an AC ID range, and the E6-T4 requirement blanket names every group including WHK;
- the deferred-scope lists in the roadmap and future-work are aligned (RSS, attachment indexing, snapshot naming).

**Context.** Each item was confirmed against the source documents in the 2026-08-19 multi-agent design audit and the follow-up manual re-verification.

## D-013 - 2026-08-19 - Route failure budget is an explicit field

**Decision.** Route dispatch configuration gains `failure_budget` (integer, 1 through 10, required): the maximum number of consecutive failed or canceled accepted tasks before the route moves to `UNCERTAIN`. A completed task resets the count. The failure-path references in the persistence state-machine note, feedback-loop §7, and the `work fail` contract now cite `failure_budget` instead of reusing `execution_hints.max_attempts`.

**Context.** D-009 bound the route's consecutive-failure budget to the Hermes execution hint, giving one field two meanings that can diverge: the hint advises the runtime about execution retries inside Hermes, while the budget governs how many follow-up intents Agent Dispatch creates after accepted work fails.

**Consequences.** The three budgets are now distinct and documented together in `configuration-spec.md` §9: `submission_retry.max_attempts` (delivery attempts for one intent), `execution_hints.max_attempts` (Hermes execution hint), and `failure_budget` (consecutive accepted-work failures). `failure_budget` is behavior-affecting and participates in the route revision digest. Config schema, both config examples, the domain-model Route, and the revision-input list are updated.

## D-014 - 2026-08-19 - Bundled consistency errata completing D-009 and D-012

**Decision.**

- `architecture-overview.md`'s component table no longer labels the Watchman environment as trusted, completing D-012's mermaid change; the adapter's responsibility now states that source binding is verified against configuration.
- `hermes-integration.md` §4 splits numeric limits out of `SinkCapabilities` into a separate `SinkLimits` block, completing D-012's capability-shape fix so the logical description matches `sink-adapter-contract.md` and `hermes-capability-report.schema.json`.
- `work-receipt.schema.json` constrains `failure_code` to the closed set from D-009 (`agent_error`, `canceled`, `timeout`, `environment_error`, or null) and requires a non-null code from that set when `status` is `failed`, matching the closed-registry discipline of the CLI error model.

**Context.** A re-verification of the 1.0.2 changes found these three items applied to only one of the documents each decision touched.

**Consequences.** No behavioral change beyond the schema enforcement; the examples still validate.

## D-015 - 2026-08-20 - E1-T1 owns the Makefile verification entrypoint, the Go schema validator, and gaori retargeting

**Decision.** E1-T1 introduces the Makefile as the single deterministic verification entrypoint, with explicit targets for the package checks (manifest checksums, schema/example validation, traceability regeneration). Schema and example validation moves to a Go implementation using a standard Draft 2020-12 validator (for example `santhosh-tekuri/jsonschema`), replacing `docs/scripts/validate-json-schemas.py`; its self-test cases are migrated to Go unit tests. `.gaori/tester.yaml` commands are re-targeted from raw `bash -c` argv to the Makefile targets, so gaori evidence capture tracks the entrypoint instead of duplicated command text. The Python validation scripts are retired once their coverage is migrated.

**Context.** The E0 audit remediation left a stdlib-only Python subset validator with accepted deferrals (subset-enumeration duplication, no static `$ref` target resolution, only the `date-time` and `uri` formats). The implementation language is Go (ADR-0004), and E1-T1 already required deterministic verification commands and schema/example validation in CI, so the replacement lands in the task that creates the Go module. The gaori configuration added during the E0 closeout wraps raw commands, which would silently drift from the documented verification commands once those become Makefile targets.

**Consequences.** The Python dependency for document validation disappears at E1-T1, discharging the deferred standard-validator items in `VALIDATION.md`. Until E1-T1 completes, the current Python commands and gaori configuration remain the authoritative reproduction path.

## D-016 - 2026-08-20 - Usage-class error codes registered for the E1-T1 CLI surface

**Decision.** The closed v0.1 error-code registry gains its first usage-class (exit 2) codes: `command_unknown` for an unrecognized command, `command_not_implemented` for a command defined by the CLI contract but not yet built, and `flag_invalid` for argument errors. `internal_unclassified` (exit 40) gives the required panic-recovery path a legal code. The minimal E1-T1 CLI emits only these four codes plus success; the registry stays closed and future codes remain contract changes recorded here and in the changelog.

**Context.** The registry predated any executable CLI and contained no usage-class codes, so the first real `agent-dispatch` binary had no legal encoding for its failure modes; emitting unregistered codes would violate error-model §4 ("implementations emit only the codes below").

**Consequences.** SOT 1.0.4. `version` and `init` are the first implemented commands (init landed with E1-T2); every other tree entry fails closed with `command_not_implemented` instead of silently succeeding.

## D-017 - 2026-08-23 - Compliance review accepted; remediation epic E7 and the SCP-008 verification exception

**Decision.** The 2026-08-22 MVP compliance review (`docs/reports/mvp-compliance-review-2026-08-22.md`, admitted into the manifest-verified package by this decision) is accepted in full. The roadmap gains remediation epic E7 with twelve tasks (the roadmap now has 8 epics and 45 tasks) covering the review's Blocker, High, Medium, and Low/Info findings and the v0.1.1 patch release. Affected Completed tasks are not reopened under task-execution-rules §4; E7 is the formal audit remediation cycle, and each E7 task appends `Audit remediation (compliance review 2026-08-22):` evidence notes to the affected tasks instead. SCP-008 is recorded as an explicit exception for v0.1.1: verification runs on macOS (darwin/arm64) only, the linux-amd64 artifact continues to ship, and no document may claim a Linux verification that was not performed; every surviving "Linux CI leg" claim is corrected by E7-T1.

**Context.** The review verified the shipped package against the SOT and found 14 MUST requirement gaps (three Blockers: expired-submitting recovery unwired, rerun creating a second authoritative task, follow-up intents never activating; five High: submit-path revalidation tautological, path facts never feeding dispatch, the unperformed Linux verification, always-skipped or hollow gate tests, and the unimplemented `config show`), one unrecorded SHOULD exception (SEC-008), and AC-102/AC-203/AC-505 GAP with AC-207 not evidenced. `required-spec.md` §1 makes every MUST blocking for v0.1, so the recorded v0.1.0 "all MUST requirements pass" claim does not hold until E7 completes.

**Consequences.** The roadmap summary, task index, and traceability matrix are regenerated for 45 tasks. E7-T12 records the MUST-closure matrix; until then VALIDATION and the roadmap describe the review findings as open. The v0.1.0 artifacts and gates stand as shipped for the single-generation happy path; v0.1.1 supersedes them after E7. SOT 1.0.13.


## D-018 - 2026-08-23 - Consolidated low and informational finding dispositions

**Decision.** Every Low and Info finding of the 2026-08-22 MVP compliance review (section 5, T1 through T8 and code hygiene) is dispositioned here in one record. Items marked FIXED were remediated during E7 (the owning task is named); items marked ACCEPTED are recorded as documented reduced guarantees with their rationale. The epic validation audit reconciles this record against the tree.

**Context.** The review's Low and Info findings were observations with narrow blast radius; fixing every one inline would have spread the remediation thin, while leaving them unrecorded would repeat the M-31 pattern the E7-T1 task existed to close.

**Consequences.** The disposition inventory (finding coordinate, disposition, owner/rationale):

- T1/Low AC-106 assertion form ("changed" vs "read"): FIXED during E7-T11 - see the dedicated AC-106 entries below.
- T1/Low fixture gaps for overflow/fresh-instance files: ACCEPTED - the environment-variable fixtures drive the same code paths deterministically; fixture files would duplicate the env switches without new coverage.
- T1/Low `WATCHMAN_RELATIVE_ROOT` recorded-not-applied: ACCEPTED as a reduced guarantee - the flag round-trips into the observation record for audit; applying it to path resolution is a post-v0.1 behavior (future-work watchman semantics).
- T1/Low path error codes (path_traversal_rejected et al.) and the payload-traversal class-4 mapping: ACCEPTED - the emitted codes (source_unsafe_path, class 30) carry the same semantics; the unused aliases stay unregistered (the registry is closed).
- T1/Low per-path exclusion reason computed-then-discarded, uncertain-delete reason not displayed: ACCEPTED - the batch-level dropped record (E7-T3) carries the machine-readable reasons; per-path display is post-v0.1 CLI polish.
- T2/Low derived IDs are string concatenation, not UUIDv7 (domain-model section 14): ACCEPTED for v0.1 - every derived ID embeds its lineage (dispatch, attempt, receipt, followup, rerun) which the audit trail depends on; the UUIDv7 rule is honored by the leaf generators (observations, batches, decisions). Post-v0.1 can move derived IDs to UUIDv7 with explicit lineage columns.
- T2/Low JCS encoder \b/\f divergence and Unicode normalization policy: ACCEPTED - the stored payloads never contain those control characters (the bounded renderers exclude them); the divergence is unreachable. Recorded as a JCS edge note.
- T2/Low reason-code registry undocumented (`meaningful_markdown_change` never emitted): FIXED in effect - the emitted reason set is now pinned by the planner propagation tests and the E7-T3/E7-T6 evidence; the registry stays the required-spec table.
- T2/Low CLI record `schema_version` fields absent on attempt/receipt records: ACCEPTED - the store validates versions on read (E7-T8) and internal drift tests pin the supported range; per-row duplication adds no consumer value. The v0.1.6 compact version identity intentionally exposes no schema range.
- T3/Low `AcquireLease`/`TransitionIntent` public methods bypass guards: ACCEPTED - they are the transactional primitives the guarded flows compose; no external caller exists (verified by the audit).
- T3/Low prune audit `transition_id` NULL: ACCEPTED - the prune audit row names the actor and cutoffs in its context; a synthetic transition id adds nothing.
- T3/Low lease TTL hardcoded one minute undocumented: FIXED during E7-T11 - the one-minute TTL is documented here (the runtime's LeaseTTL constant; the E7-T2 evidence had not named it).
- T4/Low `lookup_by_idempotency_key` declared vs runtime unsupported: ACCEPTED as an honest boundary - the capability vocabulary is closed (HER-004) and the report records the honest false; the route gate now validates requirements offline (E7-T9).
- T4/Low archive idempotency-key release caveat and Kanban CLI auth model not carried in the report: ACCEPTED - the runbook documents the archive caveat; the Kanban auth model is a Hermes-side property the public-interface report deliberately does not assert.
- T4/Low sink-contract section 9 unreachable branches (kanban accepted-non-durable, definite rejection): ACCEPTED - the branches exist for contract completeness; the adapter's honest capability set makes them unreachable by construction.
- T4/Low Watchman lifecycle client cmd.Dir/output-bound absent: ACCEPTED as a reduced guarantee - the lifecycle client invokes operator-configured argv only; the event-data path never flows through it (the runner boundary is enforced where event data exists).
- T5/Low `docs/examples/hermes-skill/SKILL.md` "provisional" wording: FIXED during E7-T11 - the header now states the validated E5-T2 status (the stale provisional wording survived E7-T1).
- T5/Low rerun test not asserting key difference: FIXED - TestOperatorRerunCreatesNewLineageAndKey asserts a fresh non-empty key (the original review noted only the difference-from-original was unasserted; the derived-key uniqueness is pinned by the idempotency-column uniqueness tests).
- T6/Low `parseOpsFlags` implicit ops flags and the json-only command list: ACCEPTED - the CLI tree is closed and completion derives from it; the doc gap is cosmetic.
- T7/Low webhook endpoint query token persisted in diagnostics: ACCEPTED as a reduced guarantee - the token appears only in operator-local store diagnostics (never logs), and the supported configuration shape is header-secret auth; query-token endpoints are an operator misconfiguration the doctor surfaces indirectly.
- T8/Low toolchain directive absent / reproducibility not automated / prune policy revision: FIXED in effect - the toolchain is pinned by go.mod (go 1.26.6), the double-build check is manual and re-run by E7-T12, and the prune audit carries the policy cutoffs.
- Code hygiene placeholder doc.go files and empty packages: FIXED - the layout deviations section documents each (E7-T10).
- T2/Info overflow-to-quarantine `generation_action: merge_reconcile` without pending set: ACCEPTED - the quarantine hold and the pending generation are separate records by design (the reconcile collapse owns pending); the action labels the disposition class, not the pending flag.
- T3/Info `state_directory_not_local` only on the init path (open path exits 20): ACCEPTED - the open path cannot know the operator context; init's exit 12 is the actionable surface.
- T3/Info `pending_reconcile` ABA self-healing: ACCEPTED - one-time self-healing semantics recorded in the 1.0.7 known-deferred note (E7-T10).
- T4/Info webhook malformed-output direct test absent / scope re-point refusal test absent: ACCEPTED - the conformance suite covers the strict-client paths; the malformed-output branch is unreachable behind the transport gates.
- T5/Info `route enable` refusal message printing the correct revision: ACCEPTED - the anti-accident gate prints the computed revision on purpose.
- T5/Low full-reconcile symlink/non-regular items reported as FileRegular: ACCEPTED - the enumeration reports the vault-relative fact set; symlinks are rejected by the containment resolver before fact creation.
- T6/Info doctor failure stdout `ok:true` envelope / exit 12-13 as success-envelope states / `watchman *` refusing `--output`: ACCEPTED - the CLI contract documents the doctor shape and the closed output surface.
- T7/Info `context_json` assembled with %q / config-location doctor warnings absent / full-reconcile enumeration bound absent / govulncheck advisory: ACCEPTED - bounded-operator contexts only (no untrusted free text reaches them), and the indirect x/text advisory has no reachable path.
- T8/Info prune audit without policy revision: ACCEPTED - the audit carries the resolved cutoffs; the policy revision is reconstructable from the changelog.
- T1/Low AC-106 symlink-escape assertion asserts "unchanged" not "not read" (the file-content leg): FIXED during E7-T11 - the test now also asserts the store carries no observation record for the rejected inputs (the content leg remains the read-absence form by design: reading is what must not happen).
- T1/Info fixture directory layout not per testing-strategy section 5: ACCEPTED - the strategy's fixture layout is a convention; the env-switch fixtures are the recorded exception (see the fixture item above).
- T2/Low testing-strategy section 3 policy-combination goldens (three combos) and the route-revision golden unimplemented: ACCEPTED - the planner determinism tests pin the evaluated combinations; dedicated goldens add serialization coverage only.
- T3/Low testing-strategy section 6 pragma-verification and busy-timeout store tests absent: ACCEPTED - Open verifies every pragma at runtime (a misconfiguration fails every command); a dedicated test would re-run Open.
- T6/Low the json-only command list undocumented in cli-spec / `watchman *` refusing `--output`: ACCEPTED - the completion registry is the machine-readable command surface; the doc gap is cosmetic.
- T7/Low `definiteNotSubmitted` diagnostic and the receipts-service bounded-payload boundary: ACCEPTED - the diagnostics are bounded by the sink contract; the payload bound is enforced at the store column.
- T8/Info doctor does not inspect trigger-definition drift: ACCEPTED - the lifecycle commands are idempotent and inspectable (watchman status); drift detection is post-v0.1.


## D-019 - 2026-08-23 - Compliance review report retired; the finding index moves into the decision record

**Decision.** The archived review report `docs/reports/mvp-compliance-review-2026-08-22.md` is removed from the SOT package and the manifest. Its normative content already lives in the SOT records: D-017 (the acceptance, the remediation scope, the SCP-008 exception, and the Linux diagnostic-run facts - the linux/arm64 container runs failed `make verify` with exit 2 on two darwin-only keychain tests without platform guards, with 506 passes as non-root), D-018 (the Low/Info disposition inventory), the E7 task Evidence sections in the roadmap (each remediated finding described with its fix), and the MUST-closure matrix in `docs/VALIDATION.md`. The severity-classified finding index below preserves the review's numbering for citation; the roadmap's evidence sections reference these IDs.

**Context.** The report served as the audit input for epic E7; with the epic complete, the four-round whole-epic review converged, and every finding fixed or dispositioned, the 45 KB forensic working paper duplicated content now distributed across the SOT records. Citations of the report now cite D-017; historical mentions inside D-017 and the changelog remain as written.

**Consequences.** The manifest and traceability regenerate without the report; `docs/reports/` returns to its documented role (versioned task evidence under the repository layout). The finding index (owner mapping):

| ID | One-line substance | Disposition |
|---|---|---|
| B-1 | Mid-submit process death wedged the route; no recovery wiring | Fixed: E7-T2 |
| B-2 | Rerun had no state guard; a second authoritative task per route was possible | Fixed: E7-T2 |
| B-3 | Follow-up intents were never activated or auto-submitted | Fixed: E7-T2 |
| H-1 | Submit-path revision/target revalidation was tautological | Fixed: E7-T3 |
| H-2 | Durable path facts never fed the dispatch path | Fixed: E7-T3 |
| H-3 | False Linux verification claims; unguarded platform tests | Claims fixed: E7-T1/E7-T4; runtime verification excepted (D-017) |
| H-4 | Always-skipping or hollow gate-evidence tests | Fixed: E7-T4 |
| H-5 | `config show` unimplemented; lineage and filters missing | Fixed: E7-T5 |
| M-1 | Drain submitted on disabled routes | Fixed: E7-T6 |
| M-2 | YAML `enabled` key never read | Fixed: E7-T6 |
| M-3 | Route transition audit rows unwritten | Fixed: E7-T6 |
| M-4 | Concurrent first-open migration failures | Fixed: E7-T7 |
| M-5 | WAL-open busy misclassified as fatal | Fixed: E7-T7 |
| M-6 | Stale-route edges without writers or operator exit | Fixed: E7-T7 |
| M-7 | Dead-letter work had no closure path | Fixed: E7-T7 |
| M-8 | Stored payload versions unvalidated on read | Fixed: E7-T8 |
| M-9 | Acceptance criteria unrendered in Kanban tasks | Fixed: E7-T8 |
| M-10 | `file_scope: markdown` unenforced at ingest | Fixed: E7-T9 |
| M-11 | flags_json schema violation; source position unpersisted | Fixed: E7-T8 |
| M-12 | State DB file permission posture | Posture: E7-T9 (owner-only state documented and checked; permissive file secrets fail closed) |
| M-13 | List filters and pagination missing | Fixed: E7-T5 |
| M-14 | `trace_id` never reached the envelope | Fixed: E7-T5 |
| M-15 | Retry reason misclassification | Fixed: E7-T5 |
| M-16 | `*_not_found` category mismatch | Fixed: E7-T5 |
| M-17 | Documented log events partially unemitted | Accepted posture: the vocabulary is declarative; the emitted lifecycle events carry the causal audit |
| M-18 | `merge_pending` not persisted on decisions | Fixed: E7-T6 |
| M-19 | Reprocess hardcoded its disposition | Fixed: E7-T6 |
| M-20 | Prune deleted begun work receipts | Fixed: E7-T9 |
| M-21 | Disabled-route reconciliation misclassified as storage | Fixed: E7-T9 |
| M-22 | Required capabilities unchecked at enable | Fixed: E7-T9 |
| M-23 | Doctor probe constructed with a zero manifest bound | Fixed: E7-T9 |
| M-24 | Unclassifiable filenames aborted the reconciliation | Fixed: E7-T9 |
| M-25 | File secret references without permission checks | Fixed: E7-T9 |
| M-26 | Redaction missed credential value patterns | Fixed: E7-T9 |
| M-27 | No LICENSE; no dependency-license review | Fixed: E7-T10 |
| M-28 | Probe timeouts flaked under load | Fixed: E7-T9 (test fixtures raised; configured limits unchanged) |
| M-29 | `init --state-dir` not persisted | Fixed: E7-T9 |
| M-30 | Crash-boundary coverage gaps | Fixed: E7-T4 |
| M-31 | Documentation consistency bundle | Fixed: E7-T10 |
| M-32 | Unusable-position handling conservative; signal unemitted | Position fixed: E7-T8; the conservative posture is accepted |


## D-020 - 2026-08-23 - Second compliance review accepted; remediation epic E8 and the closable Linux exception

**Decision.** The 2026-08-23 MVP compliance review of v0.1.1 (commit `f00ed30`) is accepted in full. The review itself stays outside the manifest-verified package (the D-019 precedent); its substance is recorded here and lands in the E8 task Evidence sections as remediation completes. The roadmap gains remediation epic E8 with six tasks (the roadmap now has 9 epics and 51 tasks) covering the review's Blocker, all ten High findings, the mapped Medium findings, and the documentation-truth cluster; every remaining finding carries an explicit Deferred or Accepted disposition in the index below. The TST-008 automatic-production-write gate stays disabled until E8-T1 through E8-T3 are Completed. The SCP-008/AC-505 Linux-verification exception is closable on the review's evidence (`make verify` passing in full on linux/arm64 as a non-root user at `f00ed30`, and `make test` passing on linux/amd64 as non-root); the closure executes with the E8-T6 documentation pass and the v0.1.2 release, hardening the two root-sensitive permission tests (defeated by `CAP_DAC_OVERRIDE` under root) with root self-skips and noting the still-unexercised Linux legs (the real Hermes and Watchman tests, `systemd-analyze verify`, and a full `make verify` on amd64).

**Context.** The review re-verified the shipped v0.1.1 claim that every MUST requirement is PASS or explicitly excepted, with eleven independent reviewer agents plus orchestrator re-verification of every Blocker and High finding, and found the claim does not hold: CON-003 and POL-007 FAIL, AC-502/AC-503/AC-506 FAIL, 22 MUST requirements PARTIAL on a named clause, and one Blocker (B-1: a vault edit arriving between completion and follow-up submission wedges `work complete` at exit 40 with no product exit) reproduced end to end through the CLI. The durable core, the crash and concurrency proofs, the subprocess/containment/secrets posture, the Hermes adapter, and the documentation traceability were verified solid, and E7's "always-skipping/hollow gate tests" defect is confirmed fixed (38 of 40 acceptance scenarios backed by executing tests with zero skips on the review host).

**Consequences.** The roadmap summary, task index, and traceability matrix regenerate for 51 tasks. The E7 closure claims the review refuted (the VALIDATION MUST-closure rows CON-003 and CLI-004, the AC-402/406/409/502/503/506 gate rows, the DAT-009 rationale, and the D-018 items marked "corrected" below) stand corrected by their owning E8 tasks or by E8-T6; until then this record is the authoritative statement that they are unsupportable for v0.1.1. The severity-classified finding index (owner mapping; the review's numbering is preserved for citation):

| ID | One-line substance | Disposition |
|---|---|---|
| B-1 | A change arriving while FOLLOWUP_READY wedges `work complete` (exit 40, no product exit) | Fixed: E8-T1 |
| H-1 | Follow-up chains unbounded; IDs grow per generation and wedge at ~21; same-second boundary defeats exact suppression | Fixed: E8-T1 |
| H-2 | Route revision ignores vault root and target board; acknowledged revision never re-checked at submit | Fixed: E8-T3 |
| H-3 | Prune deletes the attempts and receipts of an active, unresolved accepted dispatch | Fixed: E8-T4 |
| H-4 | Doctor exits 0 for two of AC-502's five conditions; an unreadable resource root passes | Fixed: E8-T4 |
| H-5 | Shipped v0.1.1 binaries not built from the released tree; no tag; the AC-506 test pins v0.1.0 | Fixed: E8-T6 |
| H-6 | Expired-lease recovery runs only in `dispatches drain`, which nothing schedules | Fixed: E8-T2 |
| H-7 | Capability report declares `lookup_by_idempotency_key: true`; enable gate opt-in, report freshness ignored | Fixed: E8-T3 |
| H-8 | A symlink-escaping delete dispatched without a containment check; one branch exits 40 | Fixed: E8-T5 |
| H-9 | Expected operator refusals and storage failures reported as exit 40 `internal_unclassified` | Fixed: E8-T1 (work commands), E8-T2 (operator/store arms) |
| H-10 | Documentation-truth cluster that survived E7 (VALIDATION rows, D-018 records, roadmap claims, README, contracts) | Fixed: E8-T6 |
| M-1 | LeaseTTL hardcoded one minute against an unbounded operator-configured `submit_timeout` | Fixed: E8-T2 |
| M-2 | Budget-exhausted retry_wait has no operator exit; drain skips it forever | Fixed: E8-T2 |
| M-3 | A rejected dispatch holds the route slot forever; the declared edge is dead code | Fixed: E8-T2 |
| M-4 | Migration lock staleness 30 s with a once-written mtime | Fixed: E8-T2 |
| M-5 | `exec.Start` failures classified unknown (argv-length dead-letters provably unsubmitted work) | Fixed: E8-T2 |
| M-6 | `resource_mutex` never consulted; the report `evidence` array unenforced | Fixed: E8-T3 |
| M-7 | Dry-run runs with NoFacts; `unchanged_content` unreachable on the dry-run surface | Fixed: E8-T5 |
| M-8 | Recrawl uncertainty never detected despite the documented promise | Fixed: E8-T5 (detection or recorded exception) |
| M-9 | Managed trigger omits `--config`; a custom config unloads at fire time | Fixed: E8-T5 |
| M-10 | Follow-ups carry the parent manifest and content fingerprint forever | Fixed: E8-T1 |
| M-11 | Reconcile passes the absolute vault root into the content fingerprint | Fixed: E8-T5 |
| M-12 | Reconcile on a non-enabled route exits 0 against cli-spec §9 | Fixed: E8-T4 |
| M-13 | Quarantine release copies stale route/policy revisions into the replacement decision | Fixed: E8-T5 |
| M-14 | Overflow precedence beats the protected check without a documented override | Fixed: E8-T5 |
| M-15 | DAT-009 enforced on one column with an impossible legacy-bypass rationale | Fixed: E8-T5 |
| M-16 | Published record schemas require members the CLI never emits | Fixed: E8-T5 |
| M-17 | Revision columns missing on five of nine record tables against the schema claim | Fixed: E8-T5 |
| M-18 | `config validate` checks gated on `--probe-targets` or unimplemented | Fixed: E8-T3 (probe-gated checks by default), E8-T5 (semantic checks) |
| M-19 | `state.db` created 0644; no doctor permission checks | Fixed: E8-T4 |
| M-20 | Redaction patterns miss common credential forms; the message path unsanitized | Fixed: E8-T4 |
| M-21 | CLI-007 Watchman-context refusal not implemented (structural defense only) | Deferred - post-v0.1.2 hardening; the managed trigger argv is fixed and maintenance has no `--input` |
| M-22 | OPS-006 "startup uncertainty" has no implementation, reason, or procedure | Fixed: E8-T4 (mapped or excepted) |
| M-23 | `route stale` ignores `active_stale_after` | Fixed: E8-T4 |
| M-24 | Reconciliation enumerates escaping symlinks as existing regular files | Deferred - enumeration hardening recorded for the next cycle |
| M-25 | Two probe tests remain load-sensitive under full coverage parallelism | Deferred - E7-T9 raised the fixtures; the residual timing is recorded |
| L-1 | `MakeRetryDue` mutates outside a transaction and writes no audit row | Fixed: E8-T2 (folded into the retry-path rewrite) |
| L-2 | Test-only `AcquireLease`/`TransitionIntent` exports bypass the guards | Fixed: E8-T2 (deleted) |
| L-3 | Inert config keys (`unsafe_path_action`, `git.mode`; discarded `work fail --detail`) | Deferred - touched again by E8-T3's projection change |
| L-4 | The NUL-path fixture contains the six characters `\u0000`, not a real NUL | Fixed: E8-T5 |
| L-5 | AC-106 test loop narrower than claimed; the AC-103 digest test overstated | Fixed: E8-T6 (tests widened with the evidence correction) |
| L-6 | Three golden tests silently self-heal when the golden is missing | Deferred |
| L-7 | Work commands on an unknown dispatch write no invalid-receipt audit row | Deferred |
| L-8 | A WalkDir file error records the wrong skip prefix, mislabeling siblings | Deferred |
| L-9 | `prune --dry-run --yes` executes (the flag is parsed, never read) | Fixed: E8-T4 |
| L-10 | Envelope shape divergences (PascalCase plan fields, no `dispatch_id`, global-option placement, `has_more`) | Deferred - wire-shape changes stay out of v0.1.2 |
| L-11 | `config show` rejects `--output json`; `watchman *` reject `--output` | Deferred |
| L-12 | No `toolchain` directive (floor, not pin) | Fixed: E8-T6 |
| L-13 | EvalSymlinks/OpenFile TOCTOU window; no Unicode normalization | Accepted - unchanged from the D-018 residual |
| L-14 | The Watchman lifecycle subprocess is weaker than the Hermes runner | Accepted - unchanged from the D-018 residual |
| L-15 | Secret file references: no ownership check; permissive-file rejection untested | Deferred |
| L-16 | `synchronous=FULL` applied once via Exec, not in the DSN | Deferred |
| L-17 | Declared-but-unemitted observability events; doctor findings carry no `trace_id` | Deferred |
| L-18 | No distinct policy revision (always the route revision) | Deferred |
| L-19 | The route enters ACTIVE_CLEAN with reason `dispatch_accepted` at arrival | Deferred |
| L-20 | One `VACUUM INTO` backup per pending migration unit | Deferred |
| L-21 | Thirteen registered error codes never emitted | Deferred |
| L-22 | `dispatches` usage string omits `discard`; cli-spec §2 tree omissions | Fixed: E8-T2 (usage string), E8-T6 (contract tree) |
| L-23 | Companion-skill labelled variables not emitted; no cross-check test | Deferred |
| L-24 | No "follow-up" terminology entry; state-machine diagram case divergence | Fixed: E8-T6 |
| L-25 | SCP-001 single-route certification is not a cardinality limit | Deferred (Info) |
| L-26 | `.markdown` accepted as Markdown, undocumented | Deferred |
| L-27 | GO-2026-5970 unreachable transitive advisory in `golang.org/x/text` | Deferred - bumps with the next dependency update |

Corrected D-018 records: the T4/Low `lookup_by_idempotency_key` entry ("the report records the honest false") and the T5/Low symlink entry ("rejected by the containment resolver before fact creation") state the opposite of the code and are corrected by E8-T3 and E8-T5 respectively; the T3/Info `state_directory_not_local` entry and the T8/Low toolchain entry are corrected by E8-T6 alongside the Linux record. SOT 1.0.27.


## D-021 - 2026-08-24 - Hardening epic E9 registered for the deferred inventory

**Decision.** The hardening inventory E8 recorded for "the next cycle" — the four Deferred mediums (M-16 record-schema/CLI-emission alignment, M-17 revision columns, M-21 Watchman-context refusal, M-25 load-timing residuals), the resolution remainders (M-20's map-value sanitization, M-17's actual migration beyond the corrected claim, T3-F006's transport fields in the revision projection), the seventeen Deferred lows, the member-task test-coverage deferrals dispositioned in the E8 closeout, and the documentation sub-wording items — is accepted as remediation epic E9 with five tasks (the roadmap now has 10 epics and 56 tasks). The M-8 recrawl exception stands (the production trigger path never observes the query-surface warning; detection would run on a surface nothing executes), the D-018 Accepted dispositions (L-13 TOCTOU, L-14 Watchman lifecycle subprocess) are not reopened, and the E8 round-2 T6-F003 finding (the root-level review file) is dispositioned on its invalid premise: the file is untracked by design under D-019/D-020 and never enters the manifest. E9 closes with the v0.1.3 patch release (the tagged latest release carries the hardening) and its validation review covers the diff from 148bf57 — including the two E8 correction commits — so the confirmation-evidence gap on that delta closes with it.

**Context.** Every E8 MUST blocker is closed; the remaining inventory is recorded hardening that no MUST requirement depends on, verified still-open by a fresh tree audit (D-020 rows re-checked against the code, six descoped rows corrected by the E8 correction pass first). Leaving the inventory unowned would repeat the M-31 pattern (recorded nowhere) that E7-T1 existed to close; registering it now keeps the roadmap the single authority for remaining work.

**Consequences.** The roadmap summary, task index, and traceability matrix regenerate for 56 tasks; E9's Requirements sections cite only existing requirement IDs (DAT-004/007/009, OPS-001/006/008, PTH-002, SRC-005, POL-007, SEC-006/007, CLI-007/008, TST-001/002/009, and the full BND-through-WHK set for the closeout task). v0.1.3 supersedes v0.1.2 after E9; TST-008 remains disabled in shipped defaults. SOT 1.0.36.


## D-022 - 2026-08-24 - Hardening epic E9 closed; v0.1.3 released; two posture supersessions recorded

**Decision.** Epic E9 closes 5/5 (the roadmap's 10 epics and 56 tasks are all Completed) with v0.1.3 tagged at the final tree. Every D-021 inventory item's final disposition: the four Deferred mediums, the resolution remainders, the seventeen Deferred lows, the member-task coverage deferrals, and the documentation items are Fixed in-tree (E9-T1's eleven audit findings reconciled by the validation remediation: the request object, the open outcome, null shapes, the enum-clamped dead-letter reason, the v7 backfill pin, and the wire pins); the M-8 recrawl exception and the D-018 Accepted dispositions (L-13, L-14) stand unchanged; the T6-F003 premise stays invalid (the root review file remains untracked by design). The E9-T2 audit's F003 provenance correction is recorded here: the decision log's M-24 correction note credited the full-reconcile every-symlink skip to E9-T1's schema work; the skip landed in E9-T2's reconciliation hardening. Two posture supersessions ride with this closeout: D-011's pre-gate scheduled-reconcile guidance (recipes omit `--submit` before the production gate) is superseded by the E9-T4/T2-F001 posture — the shipped recipes carry `--submit` unconditionally and the two-key gate is the safety boundary (an unacknowledged route fails closed at exit 14; a YAML-disabled acknowledged route persists decisions and submits nothing); and D-011's statement stands otherwise (retry liveness remains intentional for v0.1). The whole-epic validation (diff 148bf57..HEAD, covering the E8 correction delta) converged through three remediation rounds plus a clean confirmation round: the round-1 medium (the retention prune wedging on a terminal dispatch's begun receipt) and the typed-map denylist case-folding gap are fixed in-tree with regression tests; the round-3 lows (checklist wording, the slot-guard pin, the shared terminal predicate, the shared denylist helper) are fixed; the round-2 review-file observation is by-design. The epic validation also closed the E8 confirmation-evidence gap on the correction delta explicitly.

**Context.** D-021 registered E9 to keep the roadmap the single authority for the remaining hardening; leaving the epic open after convergence would repeat the unowned-inventory pattern the epic existed to close. The supersessions keep the decision log's append-only history honest: D-011 records what was decided then, D-022 records what supersedes it now and why.

**Consequences.** v0.1.3 supersedes v0.1.2 as the tagged latest; TST-008 remains disabled in shipped defaults (the release notes disclose it); the dependency advisory GO-2026-5970 is closed by the x/text v0.41.0 bump. SOT 1.0.42.


## D-023 - 2026-08-25 - External MVP compliance review accepted; E9 reopened with four remediation tasks

**Decision.** The 2026-08-25 external MVP compliance review of the shipped v0.1.3 tree (one High, five Medium) is accepted in full, and epic E9 is reopened under the task-execution-rules formal-audit provision with four remediation tasks (the roadmap now has 10 epics and 60 tasks). The findings and their owners: F1 (High), the computed route revision omits the webhook transport's behavior-affecting fields (`auth.type`, `auth.secret_ref`, `auth.header_name`, `idempotency_header`, `lookup_timeout`, the capability-report path, and the route's reconciliation block), so an acknowledged route stays acknowledged across authentication and deduplication changes (E9-T6); F2, `route enable` succeeds with neither executable nor capability report present, against the CLI contract's exit-3 refusal (E9-T6); F3, webhook header names validate against an incomplete grammar instead of the RFC 9110 token set and the configuration schema carries no pattern (E9-T7); F4, the pinned Go 1.26.6 toolchain is enforced by neither `make verify` nor `make release` (E9-T7); F5, the package's status surfaces contradict the decision record: `docs/README.md` reports SOT 1.0.38 and v0.1.2 as shipped, `docs/VALIDATION.md` reports SOT 1.0.36 and E9 open, the roadmap's epic summary still reads Planned against its own completed task rows, and the release checklist keeps its v0.1.1-era basis (E9-T9); F6, Linux remains a packaged and documented target although the product is macOS-only (E9-T8). This decision also records the support-policy change F6's remediation requires: darwin/arm64 is the only supported platform for the current product line, superseding SCP-008's and AC-505's Linux verification clauses; the Linux legs were never runtime-verified off macOS (D-017's recorded exception; D-020's closure rested on macOS-run checks), so the claimed support surface was wider than the demonstrated one. Historical release notes keep their Linux-artifact records unchanged; this decision is the supersession authority. E9 re-closes with the v0.1.4 patch release.

**Context.** D-022 closed E9 and released v0.1.3 as converged; the review verified that tree against the SOT and the CLI contract, and every finding was confirmed against the code before acceptance. Reopening E9 rather than registering a tenth epic keeps the remediation inside the epic the review audited, and the formal-audit reopen provision exists for exactly this transition. The macOS-only declaration rides here because F6's remediation is a specification change that needs an authoritative decision before E9-T8 can be scoped.

**Consequences.** The roadmap summary, task index, and traceability matrix regenerate for 60 tasks; E9-T1 through E9-T5 keep their Completed status and D-022 evidence, with the reopen note recording the formal-audit transition. The reopened Requirements sections cite only existing requirement IDs (POL-007, SEC-010, HER-005, CLI-008, SCP-005, SCP-006, SCP-008, TST-001, TST-009, and the full BND-through-WHK set for the closeout task). v0.1.4 supersedes v0.1.3 after the re-closure; the revision-projection widening means every acknowledged route re-acknowledges once under v0.1.4. SOT 1.0.43.


## D-024 - 2026-08-25 - Epic E9 re-closed; v0.1.4 released; the D-023 review fully dispositioned

**Decision.** Epic E9 re-closes 9/9 (the roadmap's 10 epics and 60 tasks are all Completed) with v0.1.4 tagged at the final tree. Every D-023 finding's final disposition: F1 (High) Fixed in-tree by E9-T6 (the revision projection covers the webhook delivery-evidence surface — auth type, secret reference, auth header name, idempotency header, lookup timeout, capability-report path — with the reconciliation block in and the retention block out by explicit disposition, so an acknowledged route pauses when authentication or deduplication changes); F2 Fixed in-tree by E9-T6 (the capability report is mandatory enable evidence in every target-liveness state, with the unsupported-version refusal that needs no live target and the shared guarantees closure); F3 Fixed in-tree by E9-T7 (the RFC 9110 tchar allowlist at sink construction and through the schema pattern, with configuration-spec section 5 stating the grammar); F4 Fixed in-tree by E9-T7 (go-version-check enforces the exact go.mod pin before any verify or release build step, prerequisite-ordered against make -j); F5 Fixed in-tree by E9-T9 (one SOT version, one shipped release, one E9 lifecycle state, one 60-task count across docs/README.md, VALIDATION.md, the roadmap, and the release checklist rewritten from its v0.1.1-era basis); F6 Fixed by the recorded supersession (E9-T8): darwin/arm64 is the only supported platform, with SCP-008's and AC-505's Linux clauses superseded here and the historical records standing as history. The member-task round-2 residual observations are reconciled in-tree by the E9-T9 audit (the gate-comment phrasing, the unavailable-branch weak-guarantee coverage leg, the RecordedVersionSupported boundary unit tests, the authProjection nil-equivalence pin, the grammar-test colon, and the schema-test comment wording), with the twin validation ladders across the cli/adapter boundary recorded as an architectural observation for the next hardening cycle rather than forced at closeout.

**Context.** D-023 reopened the epic the external review audited so the remediation stays inside its evidence trail; re-closing after the four tasks converged (each with two full-target review rounds clean at zero unresolved findings) keeps the roadmap the single authority. The revision-projection widening is an operator-visible change: every acknowledged route re-acknowledges once under v0.1.4, which the release notes disclose.

**Consequences.** v0.1.4 supersedes v0.1.3 as the tagged latest (one darwin/arm64 artifact set plus SHA256SUMS; the Linux artifact line ends with the D-023 policy); the release output, scheduling assets, and active documentation are macOS-only; the toolchain pin is enforced, not merely documented. SOT 1.0.47.


## D-025 - 2026-08-25 - v0.1.5 operational MVP baseline approved without Hermes modification

**Decision.** The Hermes operations feature/change request is accepted as the
complete v0.1.5 target and registered through four new sequential epics E10
through E13. The release must close correct Watchman subtree binding,
reconciliation fencing, then-current Hermes capability probing, per-destination
profile and skill preflight, discoverable setup, aggregate event fan-out,
bounded work outcomes, and durable notifications. Hermes core, source,
versions, tags, private storage, and plugin surface remain outside Agent
Dispatch authority. The integration must use the current public Hermes CLI;
profile enumeration uses bounded JSON, and profile-scoped skill availability
uses a strict fail-closed parser over the existing public `skills list`
surface. Configuration keeps `version: 1` but makes a clean cutover from the
legacy route `dispatch` block to `destinations[]`; legacy config is rejected
with regeneration guidance rather than automatically migrated. Existing
database evidence is forward-migrated and remains queryable. A valid
`partially_completed` work receipt creates one budgeted remaining-scope
follow-up; `blocked` requires manual intervention and is not auto-retried.

**Context.** v0.1.4 proves a durable single-route/single-task path, but the
request identifies two correctness defects and the missing operational layer
needed for multi-agent Wiki maintenance. The owner confirmed that there are no
deployed configuration consumers requiring a compatibility shim and corrected
an earlier planning assumption: this project was started on the explicit
condition that Hermes itself would not need to change. A source-only proposal
would leave contradictory higher-authority requirements in place, so this
decision approves SOT 1.1.0 as the planned baseline while retaining v0.1.4 as
the latest shipped release.

**Consequences.** ADR-0016 supersedes ADR-0009's route-wide serialization for
v0.1.5 with a per-destination lane; ADR-0017 through ADR-0019 freeze the
capability, reconciliation, and notification decisions. The roadmap becomes
14 epics and 75 tasks, with 60 Completed, 15 Planned, and E10-T1 next. G6
through G9 block v0.1.5. Executable schemas, examples, packaged skills, release
notes, code, artifacts, and tags are not changed by this design-baseline update;
their owning roadmap tasks must deliver them with tests. Rollback keeps the
upgraded database aside and restores the pre-migration backup with the previous
binary and config. SOT 1.1.0.


## D-026 - 2026-08-25 - Canonical documentation roles and paths adopted

**Decision.** The repository adopts one delivery scope with seven explicit
documentation role owners. The former `docs/docs/00-sot`, `01-architecture`,
`03-roadmap`, `04-implementation`, `adr`, and `future-work.md` authorities move
to `docs/specs`, `docs/architecture`, `docs/roadmap`,
`docs/implementation-tips`, `docs/architecture-decision-records`, and
`docs/todo/README.md`; `docs/deferred-feedback` becomes the sole owner of small
postponed findings. The former contracts, operations, and source directories
move to the top-level supporting collections `docs/contracts`,
`docs/operations`, and `docs/source`. `docs/README.md` owns the role map,
precedence, language, roadmap namespace, and repository-native validation
entrypoint.

**Context.** D-025 already provides a complete v0.1.5 planned baseline through
stable requirements, gates G6 through G9, and roadmap epics E10 through E13.
The previous nested paths were authoritative but did not expose the seven
semantic roles to conservative structural inspection, and the root index
duplicated extensive delivery history instead of serving as a bounded authority
map. Moving the established owners is therefore safer than creating forwarding
indexes or competing authorities.

**Consequences.** All tracked live references move atomically to the new paths.
The established `E<n>` and `E<n>-T<n>` identities, their allocation rules, all
14 epics and 75 tasks, task ordering, dependencies, statuses, and completed
history remain unchanged. The roadmap continues at E10-T1 with no active task;
v0.1.4 remains the shipped release and v0.1.5 remains planned. No runtime code,
schema shape, example contract, packaged skill, artifact, tag, or Hermes surface
changes. SOT 1.1.1.

## D-027 - 2026-08-31 - v0.1.6 operational reliability follow-up approved

**Decision.** The focused deployment follow-up discovered with Agent Dispatch
v0.1.5, Hermes Agent v0.20.5, Watchman 2026.07.27.00, a named non-default
board, and a named Wiki-maintenance profile is adopted as the complete v0.1.6
target. Four sequential epics E14 through E17 own guided setup and disabled
baseline safety, optional Hermes mutex compatibility with Agent Dispatch
serialization groups, automatic durable notification draining, and final
documentation/cold-validation/release proof. The detailed planned contract is
`v0.1.6-operational-follow-up.md`. ADR-0020 through ADR-0022 fix the baseline,
serialization, and notification-drain decisions. The separately requested
general `make verify` remediation is excluded; the release still runs the
existing verification gate and reports an unrelated failure without absorbing
its repair.

**Context.** A real v0.1.5 deployment exposed three remaining operational
gaps. Setup omitted the selected route from Watchman status and could not rerun
after Watchman materialized disabled runtime state. Hermes v0.20.5 became the
minimum supported release, retained the required durable public Kanban surface,
and removed `--mutex-key`, while the existing downgrade did not express or
enforce cross-destination resource serialization. Notification intents and
delivery were durable but ordinary operation required a separate remembered
drain command. None of these gaps requires changing Hermes or weakening the
production gate.

The approved follow-up is refined before E15 implementation: same-resource
destinations default to one resource-derived local group; explicit group splits
require `allow_cross_group_concurrency` and production re-acknowledgement;
existing `mutex_key` is a deprecated local alias; local enforcement is always
mandatory and a future target mutex is defense-in-depth. Migration collisions
preserve history, select no arbitrary holder, and block new group work until
allowed existing-work exits resolve them. Alias conflicts fail, the exact
default is `resource:<resource_id>`, and existing below-floor configuration
fails closed until the atomic target-floor helper updates it. Notification
after-command delivery gains a required fifteen-minute recovery schedule, one
fair ten-second command budget, due-only manual behavior, explicit immediate
retry, persisted symmetric jitter, fenced claims, and a deterministic managed
launchd identity with safe lifecycle and bounded rotation. The
E15 implementation retires the previous Hermes baseline from all current
tracked files, including historical documents, fixtures, and release notes,
while Git history and tags remain unchanged.

**Consequences.** The roadmap becomes 18 epics and 89 tasks: the shipped
v0.1.5 baseline remains 75/75 Completed, the 14 E14-E17 tasks are Planned,
E14-T1 is next, and gates G10 through G13 block v0.1.6. Configuration remains
version 1 with additive planned fields; SQLite remains forward-only; current
schemas, examples, code, tests, validation evidence, release artifacts, tags,
and production state do not change in this design adoption. SOT 1.2.0.

## D-028 - 2026-09-08 - Absolute Watchman watch-root binding adopted

**Decision.** The managed Watchman binding binds the configured absolute
resource root itself as the watch root. Installation uses `watch` (not
`watch-project`), never sends `relative_root` on the managed trigger
definition, and fails closed with actionable unwatch guidance when the
server cannot establish the configured root as its own watch root (the
typical cause: a parent directory is already a watch root). Dispatch
accepts a live invocation only when the canonicalized `WATCHMAN_ROOT`
equals the configured resource root; a present `WATCHMAN_RELATIVE_ROOT`
is rejected as a stale relative-root trigger and never acts as a second
binding axis. The `watch_bindings.relative_root` column stays with the
constant `.` as a schema-vestigial value. Epic E18 delivers the reversal
and gates it at G14; v0.1.6 remains the shipped release and E18 claims
no release.

**Context.** The operator's production host already watched
`/Users/draccoon/Workspace/Hermes` before installation, so the E10-T2
`watch-project` resolver bound that ancestor with `relative_root:
vault/Hermes`. Live events then died as `source_binding_mismatch`
while manual exact-root dispatch ingested, and `watchman test` (which
contacts no server) could not expose the gap. A controlled probe on
the live server (2026.07.27.00, frozen as
`trigger-invocation-environment-e18.txt`) shows the failure was not a
missing environment member: a relative-root trigger does set
`WATCHMAN_RELATIVE_ROOT` to the absolute subdirectory path, matching
the E0-T5 evidence. The defect is the ancestor arm itself — acceptance
depended on a persisted record plus case-sensitive string equivalence
across three separately spelled paths (the environment watch root, the
server-canonicalized relative root, and the configured root), and on
this case-insensitive APFS volume an operator-configured root spelled
with a different case than the canonical spelling can never bind. The
product intent is one operator-configured absolute directory as the
watch root: the word "vault" is not special, there is no
ancestor-watch binding, and include/exclude patterns stay scoped to
`**/*.md` under the configured root.

**Consequences.** SRC-009 and SRC-011 are amended, SRC-013 is added, and
TST-010's evidence coverage is restated for the exact root; AC-601 is
amended and gate G14 is registered (AC-1401 through AC-1403). The roadmap
becomes 19 epics and 91 tasks with E18 Planned. Existing
`source_observations` rows and content fingerprints are unaffected (no
live observation was ever persisted under the ancestor binding — every
live event failed validation); reinstallation replaces the
persisted ancestor binding row and the install topology self-heal removes
the stale ancestor trigger. E18-T2 re-binds the production route on the
operator host under explicit operator authorization: D-028 waives the
E17-T2 no-production-state boundary for that task's Watchman topology,
binding, and observation changes only, and no Hermes core, plugin,
profile, or vault-content change is authorized. SOT 1.4.0.
